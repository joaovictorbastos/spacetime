generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id   String @id @default(uuid()) // uuid significa que ele vai criar um id unico universal para cada user
  name String
}
